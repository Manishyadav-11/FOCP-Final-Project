# Week 8 – Basics of I/O and File Handling (Python)

## Input & Output
- **Input**: `input()` function for user input.
- **Output**: `print()` function; can be formatted for readability or external systems.
- **f-strings** (Python 3.6+):
  ```python
  print(f"Name: {name}, Age: {age}")
  print(f"Area: {width*height:.3f}")  # 3 decimal places
  print(f"{name:>20} - {age:>20.2f}") # right-align, width 20, 2 decimals


